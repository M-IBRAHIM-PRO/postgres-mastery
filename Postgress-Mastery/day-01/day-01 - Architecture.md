```
Cluster (ibrahim)
 └── Database (teamsync)
      ├── auth schema
      │    └── users table
      └── projects schema
```

This note is only the Day 1 structure map. For the definitions behind each level, see [[concepts#Concept 1 — Cluster|Cluster]] and
[[concepts#Concept 2 — Schemas|Schemas]].
